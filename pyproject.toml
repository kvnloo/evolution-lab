[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "evolution-lab"
version = "0.1.0"
description = "Experiment engine: genomes, Hermes recovery gym, Pareto/MAP-Elites"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "kvnloo" }]
dependencies = ["numpy>=1.26"]

[project.optional-dependencies]
openjev = [
  "openjev-phase1 @ git+https://github.com/kvnloo/openjev.git",
]

[project.scripts]
evolution-lab = "evolution_lab.cli:main"

[project.urls]
Homepage = "https://github.com/kvnloo/z0int"
Issues = "https://github.com/kvnloo/z0int/issues"

[tool.setuptools.packages.find]
include = ["evolution_lab*"]
